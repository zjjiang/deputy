## 1. 实现

- [x] 1.1 `app/web/index.html`：加 `.kind-badge` 样式（弱化、小字号）。
- [x] 1.2 `renderBoard` 卡片头部：项目名旁按 kind 渲染 `💰 商业` / `🔧 工具` 角标。

## 2. 验证

- [x] 2.1 `node --check` 抽出的内联 JS 语法通过。
- [x] 2.2 headless 渲染确认：biz 项目出现"商业"、tool 项目出现"工具"角标。

## 3. 收尾

- [ ] 3.1 开分支 → PR → 合 main。
- [ ] 3.2 归档 change。
